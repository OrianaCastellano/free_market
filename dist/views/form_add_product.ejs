<!DOCTYPE html>
<html lang="en">

<head>
  <%- include('snippets/head'); -%>
  <title>Agregar un producto</title>
</head>

<body>

  <%- include('snippets/header'); -%>

  <div class="cmp-header-title">
    <h1 class="obj-title-section">Crear producto</h1>
    <p>En este espacio puedes crear lo que quieres publicar</p>
  </div>

  <form class="cmp-form" action="/add_product" method="post">

    <div class="cmp-form__box">
      <label class="cmp-form__label">
        Titulo producto
      </label>
      <input
        class="cmp-form__input"
        type="text" 
        name="product_title" 
        required
      >
    </div>

    <div class="cmp-form__box">
      <label class="cmp-form__label">
        Descripción producto
      </label>
      <textarea
        class="cmp-form__input"
        name="product_description"
        cols="30"
        rows="10"
      >
      </textarea>
    </div>

    <div class="cmp-form__box">
      <label class="cmp-form__label">
        Imagen producto
      </label>
      <input
        class="cmp-form__input"
        type="file" 
        name="product_image"
      >
    </div>

    <div class="cmp-form__box">
      <label class="cmp-form__label">
        Precio producto
      </label>
      <input
        class="cmp-form__input"
        type="text" 
        name="product_price" 
        required
      >
    </div>

    <div class="cmp-form__box">
      <label class="cmp-form__label">
        Categoria producto
      </label>
      <select class="cmp-form__input" name="tbl_category_category_id">
        <% for( let i = 0; i < data.length; i++ ) { %>
          <option
            value="<%= data[i].category_id %>"
          >
            <%= data[i].category_name %>
          </option>        
        <% } %>
      </select>
    </div>
    <input
      class="cmp-form__input"
      type="hidden" 
      name="tbl_user_user_id"
      value="<%= id_user %>"
    >

    <div class="cmp-form__action">
      <button class="obj-btn" type="submit">
        Crear producto
      </button>
    </div>
  </form>
</body>

</html>