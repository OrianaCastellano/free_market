<!DOCTYPE html>
<html lang="en">

<head>
  <%- include('snippets/head'); -%>
  <title>Producto nombre producto</title>
</head>

<body>
  <%- include('snippets/header'); -%>

  <div>
    <h1> <%= data.product_title %> </h1>
    <p> <%= data.product_description %> </p>
    <ul>
      <li>Categoria: <%= categoryProduct %> </li>
      <li>Precio: <%= data.product_price %> </li>
      <li>Puntos totales: <%= sumQualification %> </li>
    </ul>
  </div>

  <a href="/form_review/<%= data.product_id %>">Crear una reseña</a>

  <h2>Reseñas de usuarios</h2>
  <div>

    <% for( let i = 0; i < reviewProduct.length; i++ ) { %>
      <article>
        <h3>
          <span><%= i + 1 %> </span>
          <%= reviewProduct[i].review_title %>
        </h3>
        <strong>puntos: <%= reviewProduct[i].review_qualification %> </strong>
        <p><%= reviewProduct[i].review_description %></p>

        <a href="/list_request/<%= reviewProduct[i].review_id %>">Ver respuestas de esta reseña</a>
      </article>
    <% } %>

  </div>
</body>

</html>